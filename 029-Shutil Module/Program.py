#!/usr/bin/python3
# Shutil module

"""
>>>> Read as "sh util"
>>>> Shutil is used for file transferring operations.
>>>>
>>>>
>>>>
"""
import shutil

'''

# File copying
shutil.copy(source, dest) -> For copying the files | source -> filename | dest -> folder

# File copying and renaming
shutil.copy(source, dest) -> For copying and renaming the files | source -> filename | dest -> folder along with new filename.

# Copying the entire folder structure
shutil.copytree(source, dest)

# Moving files
shutil.move(source, dest)

# Renaming files
shutil.move(sourceFileName, newFileName)

'''

print(" ")
